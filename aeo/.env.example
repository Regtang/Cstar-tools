# 复制为 .env 并按需修改（docker-compose 会自动读取）

# 令牌签名密钥（生产必改！可用 `openssl rand -hex 32` 生成）
AEO_SECRET_KEY=please-change-this-to-a-long-random-string

# 默认管理员初始密码（仅首次初始化数据库时生效）
AEO_ADMIN_PASSWORD=ChangeMe@2026

# 数据目录（容器内路径，已挂载到数据卷，一般无需改）
AEO_DATA_DIR=/data

# 前后端分离/跨域时，允许的前端来源（逗号分隔）。同源部署留空即可
AEO_CORS=
