# 喜事达工具平台 · 项目交接说明（续接用）

> 用途：在新会话里把本文件发给 Claude（或让它读取本文件），即可无缝接续，无需携带长历史。
> 最后更新：2026-06-13

---

## 一、平台概况

- 线上地址：**https://bot.cstar.com**（主域名，微信内可达，已备案）。原域名 bot.regs.com 已于 2026-06-13 整体迁移到 bot.cstar.com，后续一律以 **cstar.com** 为准（regs.com 未备案会被微信拦截，已弃用）。证书：cstar 用 Let's Encrypt 自动续期；nginx 配置 botcstar.conf。
  - 注意：本文档中凡平台访问地址均指 bot.cstar.com；另有一个税则数据源 API 是 `www.cstar.com/findRateDeclByPage`（见第六节），那是外部数据接口、与平台域名无关，勿混淆。
- 服务器：腾讯云香港轻量 **ubuntu@43.129.179.78**（Ubuntu，Docker + nginx 反向代理）
- 架构：FastAPI + SQLite + Docker compose；nginx 路由 `/`（首页/工具静态）、`/aeo/`（AEO 平台）、`/api/`（后端）、`/tools/`、`/packer/`（装箱软件）
- 数据库：SQLite 持久化在 Docker 卷 `/data`（含用户、AEO 标准、工具注册表等）

## 二、本地代码与运维位置（均在桌面）

- `~/Desktop/xishida-aeo/`：**主代码**
  - `backend/`：FastAPI（main.py、seed.py、models.py、auth.py、database.py）
  - `frontend/`：AEO 平台 SPA（index.html、app.js、styles.css、version.js、VERSION）
  - `portal/`：平台首页与各工具单文件（index.html、admin.html、submit.html、cstar-ui.css、cstar-tool.js、各 *-app.html）
- `~/Desktop/cstar-tools/`：**GitHub 仓库**（origin: github.com/Regtang/Cstar-tools，分支 main，PAT 已存钥匙串）。结构镜像：`aeo/`(=xishida-aeo)、`packer/`、`前端标准/` 等
- `~/Desktop/喜事达运维/`：**一键运维**
  - `部署.command`：双击 = 上传 backend+frontend+portal 到服务器 → docker 重建 → 健康检查。**自动升级版本号**（读 frontend/VERSION +1，生成 version.js，给 index.html 的 css/js 加 ?v= 刷缓存）
  - `同步GitHub.command`：双击 = git add/commit/push 到 GitHub
  - `cstar-deploy.log` / `cstar-sync.log`：结果日志（Claude 可读）
- `~/Desktop/cstar-tool-standard/` + `cstar-tool-standard.skill`：前端统一标准技能（v2.0）

## 三、部署 / 同步方式（重要）

1. 改完 `~/Desktop/xishida-aeo/` 里的代码后，**双击 `~/Desktop/喜事达运维/部署.command`** 上线（约 30 秒，工具数应为 9）。
2. 把改动同步到仓库：把改的文件 `cp` 到 `~/Desktop/cstar-tools/` 对应位置，**双击 `同步GitHub.command`** 推送。
3. **已知问题**：国内→GitHub 的 443 端口偶发 SSL 超时，推送会失败；**本地提交不会丢，多双击一两次即可补推**。同理香港服务器→www.cstar.com 也偶发超时（已加重试）。
4. 缓存：index.html 已设 no-cache，资源带 ?v=版本号；用户首次看旧版只需 **Cmd+Shift+R** 强刷一次。

## 四、上线工具清单（9 个 + AEO 平台）

| 工具 | 版本 | 入口 | 说明 |
|---|---|---|---|
| AEO 认证管理平台 | 前端 v1.7 | `/aeo/` | 见第五节 |
| Cstar 装箱软件 | — | `/packer/` | 设计基准（深蓝+玫红+青）|
| 危化判定 MSDS/UN | v1.1.0 | `/msds-un-app.html` | 自带样式，已改新配色 |
| 出口海关研判助手 | — | `/export-customs-app.html` | 自带样式，已改新配色 |
| 集装箱设备交接单 EIR | v1.2.0 | `/container-eir-app.html` | ISO6346、CEDEX、水印、签名、二维码 |
| 进出口税费试算器 | v1.0.0 | `/duty-calc-app.html` | 进口税费/出口退税估算 |
| HS编码·申报要素速查 | **v3.0.0** | `/hs-lookup-app.html` | **已接全量 API**，见第六节 |
| 发票·装箱单生成器 | v1.0.0 | `/trade-docs-app.html` | CI/PI/装箱单，中英双语，金额大写 |

- 所有工具署名 **作者：老瑞**；共享样式 `/cstar-ui.css`、共享库 `/cstar-tool.js`（Cstar.* 数据存储/校验/签名/导出/署名等）

## 五、AEO 平台关键状态

- **标准已升级到海关总署公告 2026 年第 34 号**（2026/4/1 施行，废止 2022 年第 106 号/114 号；信用办法海关总署令第 282 号）。
- 框架：四大核心模块（内部控制/财务状况/守法规范/贸易安全）+ **附加标准（加分项）**；共 **45 条**（42 核心 + 3 附加）。
  - 财务状况改为 9 项指标（4 偿债 + 5 盈利）+ 生产型/非生产型差异化 + 达标判定规则
  - 内部控制新增「1.13 禁限及两用物项审查」
  - 守法规范扩展到更多关联主体
- 标准框架在 `seed.py` 的 `STANDARD_FRAME`，启动时版本守护迁移（检测旧版自动刷新，状态重置为待评估）。
- 新增 3 功能：自评报告一键导出（标准自评页）、附加标准加分测算（驾驶舱卡片）、法规与政策库（侧栏）。
- 图标：盾牌+对勾 SVG 徽标 + 人形头像，底色**纯玫红 #e84368**。
- 登录：演示管理员 `admin / admin123`；平台管理员 `pmadmin`（角色 platform，与 AEO 隔离）；管理后台 `/admin.html`。
- ⚠ 各条款为按官方政策解读如实归纳的自评清单；**逐字原文需海关公告第34号附件 PDF**（用户可提供，再逐条录入）。

## 六、HS 速查全量数据接入（最新完成）

- 数据源：喜事达自家 API **`POST https://www.cstar.com/findRateDeclByPage`**（用户提供，文档在 `uploads/findRateDeclByPage-api*.md`）。返回 goodsCode/goodsName/declNotes(申报要素，**用「|」分隔**)/各档税率/customsSupervision/inspectionDesc/goodsDesc 等。
- 后端代理：`main.py` 的 **`POST /api/hs/search`**（公开，无需登录），转发到上游，规避跨域；含重试（2 次/12 秒）；地址可用环境变量 `HS_RATE_API` 覆盖。
- 前端：HS 工具点「查」/回车 → 在线查全量；输入时本地常用库即时联想；全量无匹配（口语词如"锂电池"，官方名"锂离子蓄电池"）或超时 → 自动回退本地库并提示。
- 已实测通过：查"牛肉"返回 18 条权威结果，详情含 16 项申报要素、税率、监管 4ABEFx 等。

## 七、待办 / 注意

1. **GitHub 同步排队中**：最近几次提交（全量 API 接入、图标改色、skill v2.0 等）因国内网络多次推送失败，本地已提交；网络好时双击 `同步GitHub.command` 补推（可能要点几次）。
2. HS 全量 API 从香港访问偶发慢；如需更稳，可考虑服务器侧加缓存或确认是否有更快的接入地址/鉴权。
3. 前端标准 skill v2.0 已重打包到 `~/Desktop/cstar-tool-standard.skill`（本会话 present 卡片对桌面文件不可用，但文件已在桌面，可直接双击安装）。
4. 想把全量 8500 条做成"工具内可离线/可导出"，需另做批量抓取入库（目前是在线实时查）。

## 八、常用对接指令（给新会话）

- 部署：改 `~/Desktop/xishida-aeo/`，双击 `部署.command`，读 `cstar-deploy.log` 确认。
- 同步：`cp` 到 `~/Desktop/cstar-tools/aeo/...`，双击 `同步GitHub.command`，读 `cstar-sync.log` 确认。
- 新建工具：遵循前端统一标准 v2.0（cstar-ui.css + cstar-tool.js + 署名老瑞 + 嵌入模式 + 四项基线），注册进 `seed.py` 的 `DEFAULT_TOOLS`。

## 九、近期三个会话工作记录（2026-06-12 ~ 06-13）

> 三个会话都围绕 bot.cstar.com 平台的迭代与部署。贯穿性背景：本地仓库 `cstar-tools`，通过「喜事达运维」文件夹双击 `部署.command`（自动 bump 版本号）和 `同步GitHub.command` 运维；两个反复出现的坑——国内→香港服务器/GitHub 偶发网络握手超时（需重试，本地提交不丢）、Mac 通知中心抢前台挡点击。

### 会话一「Bot deployment」——HS 速查接入全量税则 API

- 后端新增代理接口 `POST /api/hs/search`，转发到 `www.cstar.com/findRateDeclByPage`（规避跨域、统一入口），地址可用环境变量 `HS_RATE_API` 覆盖。
- 前端 HS 工具升 v3.0：点「查」/回车 = 在线查全量税则库（编码 + 申报要素 + 各档税率 + 监管条件 + 检验检疫 + 商品描述），输入时本地常用库即时联想。
- 修复点：申报要素真实分隔符是「|」（非文档写的「;」）；上游 SSL 偶发超时 → 自动重试（最终 2 次/12 秒）；口语词查不到（如"锂电池"官方名"锂离子蓄电池"返回 0）或超时 → 自动回退本地库并提示。
- 经 v1.5→v1.6→v1.7 多次部署，实测"牛肉"返回 18 条权威结果，详情含 16 项申报要素、四档税率、监管 4ABEFx。
- 会话末把平台要点写成本文件（项目交接说明）；GitHub 同步当时排队待补推。

### 会话二「Bot deployment summary」——首页弹窗修复 + 自动上架 + 装箱软件打包

- 修复首页「提交工具更新」弹窗只显示 4 个旧工具：根因是弹窗用 iframe 内嵌 submit.html、浏览器缓存了旧页；修法是给 iframe 加缓存击穿（openTool 自动附时间戳）。部署 v1.14 后弹窗显示全部 10 个工具（含新名「出口风险研判助手」「出口HS与单证速查」「AEO高级认证财务指标合规测算工具」），并克服多次网络超时成功推 GitHub（`75a9e3d..c549390`）。
- 确认新工具自动进更新列表：① 内置工具（seed.py）部署时缺失自动补建为 online；② 同事上传的工具维护者点「批准并发布」时系统自动建 online 记录、默认可见性 both。下拉实时从 `/api/catalog` 拉 + 缓存击穿，新工具一上线即出现。例外：被设隐藏/已退役、或可见性仅内部的，公开页不显示。
- 把 `cstar-tools/packer/` 打包成 **Cstar装箱软件.zip**（192K，18 文件，含主程序 + PWA 离线 + 双语 i18n + 样例模板 + 用户手册 docx）发给同事。

### 会话三「Bot deployment fable 5」——时区修复 + 需求补充功能 + 旧设备查询体检

- 修时间显示错乱：根因容器跑 UTC 差 8 小时，全平台时间统一改北京时间（影响需求/日志/工具记录所有新数据），一次性脚本修正已有 3 条需求（中途发现 `docker exec` 漏 `-i`、stdin 没进去，补上重跑成功）。部署 v1.21，同步 GitHub（`26fc61a..f7f9d9f`）。
- 上线需求「补充说明」功能：每条需求下提交人可追加内容（带时间戳、不覆盖原文、保持公示透明）；身份识别——登录通行证凭账号、匿名凭浏览器保存的编辑密钥（匿名者仅能在原浏览器补充）。旧 3 条需求无密钥，提交人自己改不了，需后台代为处理。
- 对「旧设备查询」（tool-db0f94，当时 v1.2.4）做线上体检，列 12 项优化（按优先级）：① 页面 8MB（4 张图未压缩 base64 内嵌，应转 WebP）；② 准入初判仍靠本地关键词猜，应接 `/api/hs/search` 拿权威监管条件/检验检疫；③ HS 输入无校验；④ 估价无台账/导出；⑤ 缺本地暂存；⑥ 缺署名（`Cstar.stamp`）；⑦ 缺示例数据按钮；⑧ 外链有 Wikipedia/第三方站及 http 链接；⑨ 法规无时效标注；⑩ 转化弱；⑪ 流程可做成向导式；⑫ 移动端受 8MB 拖累。建议顺序：图片瘦身 → 接全量 API → 补标准基线。
- 该会话因所选模型不可用中断；"直接改还是发给同事"尚未执行。

### 当前衔接状态（截至 2026-06-13）

- 工具目录已较本文件第四节增长（新增 旧设备查询、规划求解工具、落箱超期费PDF生成工具、世界公共假期查询、国内整车智能配载测算、Cstar 运价智算台 等），目录为自动维护，以管理后台 `/admin.html` 的实时列表为准。
- **旧设备查询已迭代到 v1.3.0**（Fantasy/营销客服部，2026-06-13 06:32 由 admin 发布为当前版本，状态：上线、可见性通用），v1.2.4 已归档。该平台对自检通过的提交自动部署，无需手动审批。待办：对比 v1.2.4→v1.3.0 看上面 12 项清单落实了几条。
