# 喜事达工具平台 · 前端统一标准 v1.0

所有挂在 **bot.regs.com** 的工具，部署前必须满足本标准。目的是让同事各自开发的工具在配色、风格、基本功能上保持一致，给客户和内部一个统一、专业的体验。

设计基准来自门户网站与「Cstar 装箱软件」。

---

## 一、配色（以 cstar.com 品牌为准）

| 用途 | 颜色 | 变量 |
|------|------|------|
| 主操作色（按钮主色/主要 CTA） | `#e84368` 玫红 | `--cs-rose`（兼容 `--cs-red`） |
| 主操作色深（hover） | `#c72d53` | `--cs-rose-dark` |
| 辅助强调（标题/链接/状态/点缀） | `#007892` 青 | `--cs-teal` |
| 辅助强调深 / 浅 | `#005f73` / `#0a9bbd` | `--cs-teal-dark` / `--cs-teal-light` |
| 导航/页头/深色块 | `#252f45` / `#303c58` 深蓝 | `--cs-navy` / `--cs-navy-soft` |
| 正文 | `#212121` | `--cs-ink` |
| 次要文字 | `#667085` | `--cs-muted` |
| 边框/分割线 | `#d3dce6` | `--cs-line` |
| 浅块 / 页面背景 | `#f5f7fb` / `#f5f5f5` | `--cs-soft` / `--cs-page` |
| 成功 / 警告 / 危险 | `#168821` / `#ff5d00` / `#e31d1c` | `--cs-ok` / `--cs-warn` / `--cs-danger` |

字体统一：`Inter, -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", "Segoe UI", Roboto, sans-serif`
圆角：卡片 8px、输入/按钮 6px。卡片用白底 + 柔和投影 `0 16px 40px rgba(37,47,69,.12)`。

主操作色用玫红、辅助强调用青、页头用深蓝；不要使用紫色等非品牌主色作为主色调。

---

## 二、统一样式表 cstar-ui.css（必须引入）

不要自己从零写配色和组件。工具 HTML 的 `<head>` 引入共享样式表即可自动统一：

```html
<link rel="stylesheet" href="/cstar-ui.css">
```

`<body>` 加 `class="cstar-ui"` 套用统一字体与背景。常用组件类（均以 `cs-` 前缀，不会和你已有样式冲突）：

- 布局：`cs-wrap`、`cs-grid-2`（左输入右结果）、`cs-row`（两列）
- 页头：`cs-header`（深蓝渐变标题条 + 底部玫红强调线）
- 卡片：`cs-card` + `cs-card-body`
- 表单：`cs-label`、`cs-input`、`cs-select`、`cs-textarea`
- 按钮：`cs-btn` + `cs-btn-primary` / `cs-btn-accent` / `cs-btn-soft` / `cs-btn-ghost`（可加 `cs-btn-block`）
- 徽章：`cs-badge` + `cs-badge-ok/warn/danger/info`
- 提示条：`cs-note` + `cs-note-info/warn/danger`
- 表格：`cs-table`；校验错误：`cs-field-error` + `cs-error-text`

线上地址：https://bot.regs.com/cstar-ui.css

---

## 三、嵌入模式（必须支持）

首页会用弹窗以 `?embed=1` 内嵌你的工具，此时要隐藏工具自带的大页头，避免「双标题」。在 `<head>` 加：

```html
<script>
  document.documentElement.classList.toggle("embed-mode",
    new URLSearchParams(location.search).get("embed") === "1");
</script>
```

cstar-ui.css 已内置 `html.embed-mode .cs-header { display:none }`，用 `cs-header` 即自动生效。

---

## 四、起步模板

新工具直接复制 `tool-template.html` 改，它已内置统一样式 + 下面四项功能基线的参考实现。
线上可查看：https://bot.regs.com/tool-template.html

---

## 五、部署前两步（每个工具上线前必做）

### 第 1 步：前端统一
- [ ] 引入 `/cstar-ui.css`，`body` 加 `cstar-ui`
- [ ] 主操作色玫红 `#e84368`、辅助强调青 `#007892`、页头深蓝 `#252f45`，无紫等非品牌主色
- [ ] 页头用 `cs-header`，并支持 `?embed=1` 嵌入模式（无双标题）
- [ ] 字体、圆角、按钮、卡片与门户/装箱软件观感一致
- [ ] **署名 + 版本更新说明**：`Cstar.stamp({tool, version, author, changelog})` 页脚署名；**发布新版本时必须附带版本更新说明**（changelog 参数，页脚版本号可点击查看；含版本号/日期/变更点）

### 第 2 步：功能测试 + 基本功能基线（参照装箱软件）
- [ ] **示例/演示数据**：一键填入样例，方便试用与演示
- [ ] **输入校验 + 错误提示**：必填项、格式、范围校验，错误友好提示
- [ ] **导出/打印**：结果可导出（CSV/TXT）或打印
- [ ] **响应式 + 本地保存**：手机可用；输入用 localStorage 自动暂存不丢失
- [ ] 功能跑通：核心流程、边界值、异常输入都测一遍

两步都打勾后，再交给我部署到 bot.regs.com（我会复核并加首页入口卡片）。

---

## 六、部署方式（由平台维护者执行）

1. 工具 HTML 放到站点根目录 → `https://bot.regs.com/你的工具.html`
2. 在门户首页 `portal/index.html` 的 `TOOLS` 数组加一项（`embed:'/你的工具.html?embed=1'`）
3. 浏览器验证：配色统一、弹窗无双标题、功能基线可用
